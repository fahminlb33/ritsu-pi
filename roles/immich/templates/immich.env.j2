# {{ ansible_managed }}

TZ={{ timezone }}

IMMICH_VERSION=release

REDIS_HOSTNAME=redis

DB_HOSTNAME=postgres
DB_PORT=5432
DB_DATABASE_NAME={{ immich_db.name }}
DB_USERNAME={{ immich_db.username }}
DB_PASSWORD={{ immich_db.password }}
